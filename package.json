{
  "name": "@codehz/pipeline",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf dist",
    "build": "tsdown",
    "release": "bun run scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsdown": "^0.9.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}

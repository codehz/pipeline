{
  "name": "@codehz/pipeline",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf dist",
    "build": "tsdown",
    "release": "bun run scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
